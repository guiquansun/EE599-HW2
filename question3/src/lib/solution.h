#ifndef TEMPLATE_SOLUTION_H
#define TEMPLATE_SOLUTION_H

#include <string>
#include <vector>

class Solution {
public:
  void RemoveDuplicates1(std::vector<int> &inputs);
  void RemoveDuplicates2(std::vector<int> &inputs);
  void ReversedOrder(std::vector<int> &inputs);
  void RemovedOdd(std::vector<int> &inputs);
  std::vector<int> Concatenating(std::vector<int> &inputs1, std::vector<int> &inputs2);
  std::vector<int> FindUnion(std::vector<int> &inputs1, std::vector<int> &inputs2);
};

#endif